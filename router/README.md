# SuperGateway

Route to local services.

## Setup

Place cert files in the `/config` directory.

Use `config/config.json.example` as a schema and create `/config/config.json` to configure the gateway.

Then run the following command:

`python3 build_and_run_gateway.py`

## License

Apache-2.0