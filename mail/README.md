# SuperMail

Send-only mail service.

## Setup

Place cert files in the `/config` directory.

Use `config/config.json.example` as a schema and create `/config/config.json` to configure the mail.

Then run the following command:

`python3 build_and_run_mail.py`

## License

Apache-2.0